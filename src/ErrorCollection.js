class ErrorTime extends Error {
  constructor(error) {
    super(error);
  }
};

class ErrorClient extends Error {
  constructor(error) {
    super(error);
  }
};

class ErrorTime extends Error {
  constructor(error) {
    super(error);
  }
};